# Duplicate Emails

## Problem Summary
Write a query to find all email addresses that appear more than once in the `Person` table.

## Solution
```sql
SELECT email FROM Person
GROUP BY email HAVING COUNT(email) > 1;
```
