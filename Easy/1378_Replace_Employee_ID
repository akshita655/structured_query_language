# Replace Employee ID With The Unique Identifier

## Problem Summary
Write a query to display each employee's unique identifier along with their name, including employees who do not have a corresponding unique identifier.

## Solution

```sql
SELECT unique_id, name FROM Employees
LEFT JOIN EmployeeUNI ON Employees.id = EmployeeUNI.id;
```
