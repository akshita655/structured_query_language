# Customer Placing the Largest Number of Orders

## Problem Summary
Write a query to find the customer who has placed the highest number of orders.

## Solution

```sql
SELECT customer_number FROM Orders
GROUP BY customer_number
ORDER BY COUNT(*) DESC
LIMIT 1;
```
