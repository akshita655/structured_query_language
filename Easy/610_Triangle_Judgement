# Triangle Judgement

## Problem Summary
Write a query to determine whether the three given side lengths can form a valid triangle.

## Solution

```sql
SELECT *,
    IF(
        x + y > z AND y + z > x AND x + z > y, 'Yes', 'No'
    ) AS triangle
FROM Triangle;
```
