# Delete Duplicate Emails

## Problem Summary
Delete duplicate email records from the `Person` table, keeping only the record with the smallest `id` for each email.

## Solution

```sql
DELETE p1 FROM Person p1, Person p2
WHERE p1.email = p2.email AND p1.id > p2.id;
```
