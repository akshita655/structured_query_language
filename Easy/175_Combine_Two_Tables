# Combine Two Tables

## Problem Summary
Write a query to retrieve each person's first name, last name, city, and state by combining the `Person` and `Address` tables, including people without an address.

## Solution

```sql
SELECT FirstName, LastName, City, State FROM Person AS p
LEFT JOIN Address AS a ON p.PersonId = a.PersonId;
```
