# Swap Salary

## Problem Summary
Write a query to swap the gender values in the `Salary` table, changing `f` to `m` and `m` to `f`.

## Solution

```sql
UPDATE Salary
SET sex = CASE
    WHEN sex = 'f' THEN 'm' ELSE 'f'
END;
```
