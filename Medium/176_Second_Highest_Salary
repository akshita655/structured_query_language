# Second Highest Salary

## Problem Summary
Write a query to return the second highest salary from the Employee table. If no second highest salary exists, return NULL.

## Solution
```sql
SELECT MAX(salary) AS SecondHighestSalary
FROM Employee
WHERE salary < (SELECT MAX(salary) FROM Employee);
